# Global git excludes (core.excludesfile): OS and editor junk only.
# Project-type artifacts (logs, archives, build output) belong in
# each project's own .gitignore.

# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
ehthumbs.db

# Editors
*.swp
*.swo
.netrwhist
